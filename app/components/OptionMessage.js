/*
Think about how to create the guiding 'option' chat conversations,
in which the chatbot returns options that you can choose from to lead the
conversation instead of the person having to guess which way the conversation
goes into
*/

var React = require('react');
var PropTypes = React.PropTypes;
var ConversationItemActions = require('../actions/ConversationItemActions');

var OptionMessage = React.createClass({
  render: function() {
    return (null);
  }
})

module.exports = OptionMessage;
